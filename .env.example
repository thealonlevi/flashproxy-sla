# Copy to .env and fill in. Used by docker-compose and the demo prober.
# Proxy URL for the demo prober (the one package it probes locally):
ISP_PROXY_URL=http://USER:PASS@HOST:30

# Optional: Discord webhook for status-change alerts (collector).
DISCORD_WEBHOOK=
